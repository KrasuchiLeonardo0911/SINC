package com.sinc.mobile.ui.theme

import androidx.compose.ui.graphics.Color

// --- Paleta de Colores Estandarizada "SINC" ---

// Principal (Bordó)
val SincPrimary = Color(0xFF8C2218)
val SincPrimaryDark = Color(0xFF621710) // Darker shade from color-hex
val SincPrimaryLight = Color(0xFFBA7A74) // Lighter tint from color-hex

// Contenido sobre colores principales
val SincOnPrimary = Color(0xFFFFFFFF)

// Fondos y Superficies (Grises y Blanco)
val SincBackground = Color(0xFFF5F5F7) // Gris claro
val SincSurface = Color(0xFFFFFFFF)   // Blanco

// Contenido sobre fondos y superficies
val SincTextPrimary = Color(0xFF1A1A1A)     // Negro para texto
val SincTextSecondary = Color(0xFF8A8A8E) // Gris para texto secundario

// Varios
val SincDivider = Color(0xFFEAEAEA) // Gris para divisores
val SincError = Color(0xFFBA1A1A)   // Rojo para errores
